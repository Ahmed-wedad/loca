import { DefaultTheme } from "react-native-paper"
import { color } from "../../theme/color"

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "rgb(29,74,220)",
    background: "transparent",
  },
}
