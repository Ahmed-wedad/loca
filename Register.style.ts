import { StyleSheet } from "react-native"
import { theme } from "../myscreen.style"

export const registerstyle = StyleSheet.create({
  Button: {
    margin: 15,
    marginleft: 0,
    marginRight: 0,
  },
  Content: {
    padding: 15,
    paddingTop: 0,
  },
  Icon: {
    color: theme.colors.primary,
  },
})
